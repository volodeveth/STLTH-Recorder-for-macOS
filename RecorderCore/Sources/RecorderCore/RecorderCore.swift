/// Headless core of STLTH Recorder for macOS: audio capture, session storage, permissions.
/// Deliberately free of any UI dependency so it can be unit-tested in CI without
/// audio hardware.
public enum RecorderCore {
    public static let version = "0.1.0"
}
